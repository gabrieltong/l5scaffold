@extends('layout')

@section('navbar.{{classSingle}}.class')
    active
@endsection

@section('title')
    {{ trans('model.{{classSingle}}.title') }}
@endsection

@section('header')
    <div class="page-header clearfix">
        <h1>
            <i class="glyphicon glyphicon-align-justify"></i> {{ trans('model.{{classSingle}}.title') }}
            <a class="btn btn-success pull-right" href="{{ route('{{namespace}}{{class}}.create') }}"><i class="glyphicon glyphicon-plus"></i> {{ trans('manage.create') }}</a>
        </h1>

    </div>
@endsection

@section('content')
    <div class="row">
        <div class="col-md-12">
            @if(${{class}}->count())
                <table class="table table-condensed table-striped">
                    <thead>
                        <tr>
                            <th>ID</th>
                            {{header_fields}}
                            
                        </tr>
                    </thead>

                    <tbody>
                        @foreach(${{class}} as ${{classSingle}})
                            <tr>
                                <td>{{${{classSingle}}->id}}</td>
                                {{content_fields}}
                                <td class="text-right">
                                    <a class="btn btn-xs btn-primary" href="{{ route('{{namespace}}{{class}}.show', ${{classSingle}}->id) }}"><i class="glyphicon glyphicon-eye-open"></i> {{ trans('manage.view') }} </a>
                                    <a class="btn btn-xs btn-warning" href="{{ route('{{namespace}}{{class}}.edit', ${{classSingle}}->id) }}"><i class="glyphicon glyphicon-edit"></i> {{ trans('manage.edit') }} </a>
                                    <form action="{{ route('{{namespace}}{{class}}.destroy', ${{classSingle}}->id) }}" method="POST" style="display: inline;" onsubmit="if(confirm('{{trans('manage.are_you_sure')}}')) { return true } else {return false };">
                                        <input type="hidden" name="_method" value="DELETE">
                                        <input type="hidden" name="_token" value="{{ csrf_token() }}">
                                        <button type="submit" class="btn btn-xs btn-danger"><i class="glyphicon glyphicon-trash"></i> {{ trans('manage.delete') }}</button>
                                    </form>
                                </td>
                            </tr>
                        @endforeach
                    </tbody>
                </table>
                {!! ${{class}}->render() !!}
            @else
                <h3 class="text-center alert alert-info">{{ tran('manage.list.empty')}}</h3>
            @endif

        </div>
    </div>

@endsection